# build output
dist/

# local env (never commit real credentials — though the anon key is public anyway)
.env
.env.local
.env.*.local

# vercel
.vercel

# os / editor cruft
.DS_Store
*.log
node_modules/
